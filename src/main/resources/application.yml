spring:
  application:
    name: bookservice
  profiles:
    active: ${SPOTIFY_ACTIVE:dev}
  config:
    import: "configserver:http://admin:1234@localhost:9000"

logging:
  level:
    org:
      hibernate: info


#    rabbitMQ
rabbitmq:
  exchange:
    name1: reviewExchange
  queue:
    q1: reviewq
  routing:
    topickey: review.#




